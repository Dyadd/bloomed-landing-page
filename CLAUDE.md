# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This repository is in its initial state with no source code yet. The `.gitignore` suggests a Node.js and/or Python project with standard build output directories (`dist/`, `build/`).

Update this file once the project stack and structure are established.
